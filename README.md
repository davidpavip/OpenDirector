# OpenDirector Day 1 — Timestamp Core

Adds the `Timestamp` primitive.

Canonical format:

```text
HH:MM:SS,mmm
```

Run:

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -e .
pip install pytest
python -m pytest -q
```
